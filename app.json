{
  "addons": ["heroku-redis:in-dyno"],
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "psql -c 'CREATE DATABASE some_test_database;' $DATABASE_URL && psql -c 'SELECT 1;' -d some_test_database $DATABASE_URL"
      },
      "buildpacks": [
        {
          "url": "https://github.com/heroku/heroku-buildpack-go"
        },
        {
          "url": "https://github.com/heroku/heroku-buildpack-ci-postgresql"
        }
      ],
      "env": {
        "LANG": "en_US.UTF-8",
        "LC_CTYPE": "en_US.UTF-8",
        "LC_COLLATE": "en_US.UTF-8"
      }
    }
  }
}
